apiVersion: v1
kind: Service
metadata:
  name: ingress-service-proxy
  namespace: default  # Same namespace as Ingress
spec:
  type: ExternalName
  externalName: ingress-service-proxy.ingress-nginx.svc.cluster.local