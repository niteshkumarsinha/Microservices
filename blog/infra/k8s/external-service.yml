apiVersion: v1
kind: Service
metadata:
  name: ingress-service-proxy
  namespace: default  # Same namespace as Ingress
spec:
  type: ExternalName
  externalName: my-service.ingress-nginx.svc.cluster.local